name:TextButton (UART)
description:Uses two TextButton widgets ("Start" and "Stop") to send commands over UART Serial2. Callbacks send ">command:start" and ">command:stop" so an external device can receive them. ST7796, FT6336U, ESP32-S3.
