name:RectButton (UART)
description:Uses two RectButton widgets to send commands over UART Serial2. Each button sends formatted strings (e.g. ">commandA:0/1") when pressed or released so an external device can be controlled. ST7796, FT6336U, ESP32-S3.
